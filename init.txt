# Create Next.js (App Router) + TS + Tailwind
npx create-next-app@latest taskez \
  --ts --eslint --tailwind --src-dir --app --import-alias "@/*"

cd taskez

# UI + state + forms
pnpm add @tanstack/react-query zod react-hook-form motion

# shadcn/ui (Radix) baseline
pnpm add clsx tailwind-merge class-variance-authority lucide-react
pnpm dlx shadcn@latest init -d
pnpm dlx shadcn@latest add button card input label sonner sheet dialog navigation-menu

# PWA enablement (Workbox under the hood)
pnpm add @ducanh2912/next-pwa
pnpm add 